# Features

Feature folders group domain-specific UI, hooks, schemas, types, and API adapters.

Keep route files in `src/app` thin: they should compose feature components rather than hold business logic.
